import { Meta } from '@storybook/addon-docs/blocks';

<Meta title='Components/Breadcrumbs/Introduction' />

# Breadcrumbs

`<Breadcrumbs />` component is used mostly on SubHeader component.

It should always render a component (not `null`) even if there are no steps, so it does not break the layout when rendering with display flex or similar.
