import type { SxProps, Theme } from '@mui/material/styles';

export const skeletonWrapperStyles: SxProps<Theme> = {
  height: '100%',
  width: '100%',
  overflow: 'hidden',
  padding: 0,
  margin: 0,
};
