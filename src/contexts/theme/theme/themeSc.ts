import { createTheme } from '@mui/material/styles';

import { themeCore } from '../core';
import { palette } from '../palettes/themePaletteSc';

/**  South Carolina theme */
export const themeSc = createTheme(themeCore, {
  palette,
});
